google-genai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
rich>=13.0.0
flask>=3.0.0
requests>=2.31.0

# ── Live Agent 2 & 3 Dependencies ─────────────────────────────────────────────
geopy>=2.4.0              # Nominatim geocoding (OSM, free, no key)
duckduckgo-search>=6.0.0  # DuckDuckGo web search fallback (no API key needed)
httpx>=0.27.0             # Async-capable HTTP client (OSRM, future async support)
beautifulsoup4>=4.12.0    # HTML parsing for Almosafer BS4 fallback scraper
lxml>=5.0.0               # Fast HTML parser backend for BeautifulSoup
playwright>=1.40.0        # Headless browser used by link_minter to mint deep checkout URLs
